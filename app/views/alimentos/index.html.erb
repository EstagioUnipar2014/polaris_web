<h1>Listing alimentos</h1>

<table>
  <thead>
    <tr>
      <th>Descricao</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @alimentos.each do |alimento| %>
      <tr>
        <td><%= alimento.descricao %></td>
        <td><%= link_to 'Show', alimento %></td>
        <td><%= link_to 'Edit', edit_alimento_path(alimento) %></td>
        <td><%= link_to 'Destroy', alimento, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Alimento', new_alimento_path %>

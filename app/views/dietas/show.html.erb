<p id="notice"><%= notice %></p>

<p>
  <strong>Descricao:</strong>
  <%= @dieta.descricao %>
</p>
<% @dieta.ciclos.each do |ciclo| %>
  <Strong>Periodo:</strong>
  <%= ciclo.descricao %></br>
  <% ciclo.alimentos.each do |alimento| %>
    <%= alimento.descricao %>
    <% alimento.medidas.each do |medida| %>
      <%= medida.unidade.nome %>
      <%= meidade.quantidade %>
    <% end %>	
  <% end %>
<% end %>
<%= link_to 'Editar', edit_dieta_path(@dieta) %> 
<%= link_to 'Voltar', dietas_path %>

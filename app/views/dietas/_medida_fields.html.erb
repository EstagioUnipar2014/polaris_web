<div class="field medida-partial">
  <%= f.label "Quantidade" %><br>	
  <%= f.text_field :quantidade %>
  <%= f.collection_select :unidades_id, Unidades.all, :id, :abreviacao %>
  <%= f.collection_select :alimento_id, Alimento.all, :id, :descricao %>
  <%= f.link_to_remove "Remover alimento"%>
</div>

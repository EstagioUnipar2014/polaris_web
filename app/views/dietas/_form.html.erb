  <%= nested_form_for(@dieta) do |f| %>
    <% if @dieta.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@dieta.errors.count, "error") %> prohibited this dieta from being saved:</h2>

        <ul>
        <% @dieta.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <%= f.hidden_field :propriedade_id, :value => current_usuario.propriedade_id %>

    <div class="field">
      <%= f.label 'Dieta do dia' %><br>
      <%= f.text_field :descricao %>
    </div>
    
    <%= f.fields_for :ciclos %>
    <p><%= f.link_to_add 'Adicionar período', :ciclos %></p>
    
    <div class="actions">
      <%= f.submit 'Salvar'%>
    </div>
  <% end %>


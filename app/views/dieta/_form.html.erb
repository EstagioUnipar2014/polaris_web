<%= form_for(@dietum) do |f| %>
  <% if @dietum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dietum.errors.count, "error") %> prohibited this dietum from being saved:</h2>

      <ul>
      <% @dietum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_field :descricao %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
